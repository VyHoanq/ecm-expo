import React from 'react'
import { Text, View } from 'react-native'

/**
 * Renders the Cart screen.
 *
 * @returns A React element containing a View with a Text node that displays "Cart".
 */
export default function Cart() {
    return (
        <View>
            <Text>Cart</Text>
        </View>
    )
}