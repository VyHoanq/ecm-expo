import React from 'react'
import { Text, View } from 'react-native'

/**
 * Renders the Profile screen with a single "Profile" label.
 *
 * @returns The component's root React element: a View containing a Text node with the literal "Profile".
 */
export default function Profile() {
    return (
        <View>
            <Text>Profile</Text>
        </View>
    )
}